#readme.md

- идет последовательный запрос собранных ссылок на 1001tracsklist + beatport;
- на странице поиска регулярным выражением ищется вхождение  re.compile(r'(span class=\"st\".*\bBPM\b\s\d\d\d.*?)>');
- если вхождение найдено, то это ссылка на beatport и из описания можно вынуть нужные данные
